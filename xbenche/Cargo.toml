[package]
name = "xbenche"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
clf = { path = ".." }
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "cache_bench"
harness = false
